version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm ci --only=production
run:
  command: node server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
